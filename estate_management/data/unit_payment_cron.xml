<odoo>

    <record id="ir_cron_unit_payment_due_reminder" model="ir.cron">
        <field name="name">Unit Payment Due Reminder</field>

        <field name="model_id" ref="model_estate_unit_payment"/>

        <field name="state">code</field>

        <field name="code">model.cron_unit_payment_due_reminder()</field>

        <field name="interval_number">1</field>
        <field name="interval_type">days</field>

<!--        <field name="numbercall">-1</field>-->

        <field name="active">True</field>

    </record>

</odoo>